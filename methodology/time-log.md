# Time Log — AIA Evaluation Project

> This feeds the required methodology spreadsheet (3 sheets: Time Log, Tool Usage Summary, Decision Log).
> Log each session as you go — it's much easier than reconstructing at the end.

## Format
| Date | Duration | Area | AI Tool | What AI Generated | What I Changed/Overrode |
|------|----------|------|---------|-------------------|------------------------|

## Sessions

| Date | Duration | Area | AI Tool | What AI Generated | What I Changed/Overrode |
|------|----------|------|---------|-------------------|------------------------|
| 2026-06-13 | ~1h | Planning + Scaffold | Claude Code | Full architecture plan, all scaffold files, pyproject.toml, Makefile, .env.example | Confirmed all choices; added cheap extras (anomaly highlights, forecast explain, CSV upload, Mermaid diagram) |
| 2026-06-13 | ~30m | DevOps — CI setup + GitHub push | Claude Code | GitHub Actions ci.yml (4 stages: lint→unit→integration→docker), gh CLI install, repo creation | Caught that workflow scope was missing from gh auth — had to re-authenticate manually; AI didn't anticipate this |
| 2026-06-13 | ~10m | Fix — lint errors | Claude Code | Auto-fixed 3 ruff errors (import ordering in config.py + conftest.py, unused pytest import) | No override needed; ran make lint locally, confirmed clean before pushing |
| 2026-06-13 | ~45m | Phase 1 — Mock data generator | Claude Code | generate.py (50k transactions, 2 source feeds, seasonal patterns), products.py (36 SKUs / 5 categories), stores.py (25 stores / 5 regions), 16 unit tests | Overrode: AI used Indian city names for stores (good regional flavour, kept it); AI added unused math import and long lines — caught by lint and fixed; added per-file ruff ignore for seed data files |
| 2026-06-13 | ~15m | Fix — CI unit test failure | Claude Code | Identified root causes: no tests committed yet + coverage gate on wrong step; fixed ci.yml (removed --cov-fail-under from unit step, moved gate to integration step over full suite); added test_config.py as baseline unit test; removed global --cov-fail-under from pyproject.toml | Overrode AI's initial placement of coverage gate — wrong to enforce 60% on a partial test run |
| 2026-06-13 | ~10m | Analysis — CI Integration + Docker failures | Claude Code | Diagnosed: Integration fails because generate.py/pipeline/model not committed (Phase 1 local only); Docker skipped because it depends on integration passing (GitHub Actions "needs" chain) | No fix yet — these will auto-resolve once Phase 1 is committed. Root cause: CI was written for the full system before the system existed. |
| 2026-06-13 | ~20m | Phase 1 — End-to-end verification (partial) | Claude Code | Ran generate.py on clean slate; confirmed 4 raw files: transactions_pos.csv (~33k rows), transactions_ecom.json (~17k rows), product_master.csv (35 SKUs), store_regions.csv (25 stores); verified injected quality issues present — null store/SKU, negative qty, mixed date formats ($-prefix prices, dd/mm/yyyy vs yyyy-mm-dd), zero-qty ghost orders, duplicate transaction IDs, unknown SKUs | **User correctly caught that this was not true end-to-end** — pipeline stage had not been committed yet so DuckDB loading could not be verified; AI's initial claim of completing e2e was inaccurate. Also caught: AI's "e2e steps" were manual inspection only, not automated assertions. True e2e deferred until Phase 2 was complete. |
| 2026-06-13 | ~1h | Phase 2 — Ingestion pipeline | Claude Code | 5 pipeline stages (extract, validate, transform, dedupe, load), DuckDB star schema (fact_sales + 4 dims + quality report table), 20 unit tests per stage | Overrode: AI used executescript (SQLite API) — DuckDB doesn't have it, caught and fixed immediately; ruff auto-fixed 7 import/style issues |
| 2026-06-13 | ~45m | Phase 2 — Enhancements: quarantine table + 4 new validation rules + 3 ADRs + extension guide | Claude Code | Added rejected_transactions quarantine table (full row as JSON), 4 new rules (invalid_price, zero_price, invalid_date, date_out_of_range), first-rule-wins rejection tagging, 3 ADRs (star schema, LLM interface, chatbot safety), extension guide | Fixed hidden dead-code bug in validate loop; cleaned up redundant line in date_out_of_range rule AI generated |
| 2026-06-14 | ~20m | Phase 2 — End-to-end verification | Claude Code | Ran full pipeline on clean slate: 50,650 rows extracted → 2,641 quarantined → 47,388 loaded into fact_sales; verified all 6 DuckDB tables (dim_product 35 SKUs, dim_store 25 stores, dim_date 731 days, fact_sales 47,388 rows, pipeline_runs 1 run, rejected_transactions 2,641 rows); 46/46 unit tests pass, 71% coverage, ruff lint clean | No overrides — all results matched expectations exactly |
| 2026-06-14 | ~15m | Fix — INR price correction in product master | Claude Code | Updated all 35 SKU list_prices from USD-style values (₹1.50–₹9.99) to realistic Indian Rupee price points (₹35–₹350); regenerated all mock data, re-ran full pipeline + model train | Prices chosen manually based on Indian FMCG market knowledge — AI generated the values but Shrushti confirmed they look realistic |
| 2026-06-14 | ~20m | Phase 3 — End-to-end verification | Claude Code | Clean slate → generate (50,650 rows) → pipeline (47,388 loaded, 2,641 quarantined) → verified INR prices in fact_sales (₹31–₹385 range) and revenue totals (Personal Care ₹65L, Beverages ₹34L) → trained model (MAE ₹5,251, MAPE 14.6%) → detected 18 anomalies → tested predict() for Beverages/North (₹36–37k/month) and Personal Care/South (₹67–69k/month) → 63/63 unit tests pass, lint clean | No issues found |
| 2026-06-14 | ~1h | Phase 3 — Forecasting model + anomaly detection | Claude Code | features.py (time/seasonality/lag engineering), model.py (Ridge regression, holdout eval, ±1.96σ CI, multi-step autoregressive predict), anomaly.py (z-score detection, severity tiers, DB persist), train.py (CLI: train + detect in one command); 2 new DB tables (anomalies, model_metrics); 17 unit tests; MAE 171.61, MAPE 14.8%, 17 anomalies detected | Caught bug: severity column not added to DataFrame before final column select — fixed; fixed 5 lint issues (long lines, datetime.UTC alias, import ordering) |
| 2026-06-14 | ~1h | Review pass (phases 0–3) — bug fixes + test hardening | Claude Code | Full code review of all phases. Generator now injects 5 previously-missing quality issues so all 10 validation rules fire (null txn_id, invalid_date, date_out_of_range, invalid_price, zero_price); guarded late-arriving block against null/corrupt rows; fixed datetime.utcnow() deprecation in load.py; corrected transform comment. Added 15 unit tests (null_transaction_id rule, extract_all, total recalc, 5 new generator quality checks, 6 DB-backed model train/predict/anomaly tests). Tests 63→78, coverage 63%→79%; model.py/features.py/anomaly.py/connection.py/extract.py now 100% | Found that the data-quality showcase only exercised 5 of 10 rules — the headline feature was under-demonstrated; fixed by completing the injected-issue set |
| 2026-06-14 | ~45m | Phase 4 — LLM insights + chatbot brain; CI Docker fix | Claude Code | LLMProvider abstract interface (base.py), MockProvider (deterministic, keyword-keyed canned responses), GeminiProvider (google-genai SDK), provider factory (get_provider() reads LLM_PROVIDER env var); insights.py with 6 whitelisted DuckDB queries + 4 public functions: get_summary, answer_question, explain_forecast, explain_anomaly; intent resolver maps question keywords → query set so LLM never touches raw SQL; 19 unit tests (mock provider, factory, intent resolver, all 4 insight functions with fake conn); minimal FastAPI app + Dockerfile to fix long-standing CI Docker build failure | No overrides — design (whitelisted queries, no raw SQL from LLM) was confirmed from Phase 0 ADR |

## Bugs Found and Fixed

| Date | Bug | Where Found | Root Cause | Fix |
|------|-----|-------------|------------|-----|
| 2026-06-14 | Prices in USD not INR | Noticed after Phase 3 e2e — revenue numbers looked wrong for an India-based CPG company | Data generator used Western price points (₹1.50–₹9.99 = dollar values) for a project with Indian stores | Updated all 35 SKU list_prices to realistic INR values (₹35–₹350); regenerated data and re-ran full pipeline + training |
| 2026-06-14 | Data-quality showcase exercised only 5 of 10 validation rules | Review pass — pipeline output only ever showed 5 rejection categories | Generator never injected unparseable dates, non-numeric/zero prices, out-of-range dates, or null transaction IDs; `$`-prefixed prices are valid and late-arriving rows were clamped in-range | Added injection for all 5 missing issue types so the quality report demonstrates every rule (now 10/10 fire on real data) |
| 2026-06-14 | Generator crashed on null txn_id / corrupt date | First regen after adding new injected issues | Late-arriving block did `transaction_id + "_LATE"` and `date.fromisoformat(date)` without guarding the newly-injected None ids and unparseable dates | Added guards to skip those records in the late-arriving loop |
| 2026-06-14 | `datetime.utcnow()` deprecated in load.py | Review pass | Used the deprecated naive-UTC call; inconsistent with `datetime.now(UTC)` used elsewhere | Switched both call sites to `datetime.now(UTC)` |
| 2026-06-13 | `executescript` not in DuckDB | First run of pipeline after Phase 2 | AI used SQLite's `executescript()` API which DuckDB doesn't support | Split DDL string on `";"` and call `conn.execute()` per statement in connection.py |
| 2026-06-13 | Dead branch — `unknown_sku` rule never ran | Code review of validate.py | RULES was a dict; `unknown_sku` key was absent, so the loop branch checking for it was unreachable — the rule silently never fired | Converted RULES to an ordered list of `(name, fn)` tuples; all rules now run uniformly |
| 2026-06-13 | Redundant assignment in `date_out_of_range` | Code review of validate.py | AI generated `max_date = date.today().replace(day=date.today().day)` on one line then immediately overwrote it on the next — dead code | Removed the first assignment |
| 2026-06-13 | Coverage gate on wrong CI step | CI failure — 0% coverage reported on unit step | `--cov-fail-under=60` was both in `pyproject.toml [addopts]` and on the unit test step; unit step only runs a subset of tests so coverage was 0% on an empty repo | Removed gate from `pyproject.toml` and unit step; gate now only on the integration step which runs the full suite |
| 2026-06-13 | `gh auth` missing `workflow` scope | Push to GitHub rejected | Default `gh auth login` doesn't request the `workflow` OAuth scope; pushing `.github/workflows/` requires it | Re-authenticated with `gh auth refresh -h github.com -s workflow` |
| 2026-06-14 | `severity` column missing from anomaly DataFrame | `KeyError` on first `make train` run | anomaly.py built `severity` inside the row tuples for DB insert but never added it as a column to the DataFrame before the final column select | Added `flagged["severity"] = flagged["z_score"].apply(_severity)` before the return statement |

## Key Decisions Log

| Decision | Options Considered | Why Chosen | AI Suggestion | Followed AI? |
|----------|--------------------|------------|---------------|--------------|
| Backend framework | FastAPI vs Django vs Flask | FastAPI: async, auto-docs at /docs, pydantic validation, fast to scaffold | FastAPI | Yes |
| UI framework | Streamlit vs React | Streamlit: all-Python, chat widget built-in, minimal boilerplate for a skeleton | Streamlit | Yes |
| Storage | DuckDB vs PostgreSQL | DuckDB: zero setup, file-based, excellent for analytics queries; Postgres = extension point | DuckDB | Yes |
| LLM provider | Gemini vs Claude vs mock | Gemini: free via Sigmoid Google Workspace; mock for CI/tests; abstracted behind interface | Gemini + mock | Yes |
| ML model | Linear/Ridge vs complex | "Linear regression that works beats a neural network that doesn't" (brief's own words) | Linear/Ridge | Yes |
| Chat safety | Raw SQL from LLM vs whitelisted queries | Never let LLM write/execute raw SQL; use whitelisted parameterized queries | Whitelisted | Yes (overrode naive approach) |
| GitHub auth scope | Default gh auth vs workflow scope | CI pushes .github/workflows/ files which need explicit workflow scope — GitHub blocks it otherwise | Claude didn't know upfront; discovered during push failure | No — had to manually re-auth with -s workflow flag |
